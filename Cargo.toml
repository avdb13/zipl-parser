[package]
name = "zipl-parser"
version = "0.1.0"
edition = "2021"

[dependencies]
chumsky = { git = "https://github.com/zesterer/chumsky", branch = "main", default-features = false, features = [
  "stacker",
] }
